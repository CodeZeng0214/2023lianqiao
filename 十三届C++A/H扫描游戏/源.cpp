#include <bits/stdc++.h>

using namespace std;

int n;
long long L;

int x[200005];
int y[200005];
int z[200005];
int d[200005];
int ans[200005];

int main()
{
	cin >> n >> L;
	for (int i = 1; i <= 1; i++)
	{
		cin >> x[i] >> y[i] >> z[i];
		d[i] = x[i] * x[i] + y[i] * y[i];
	}
	int tag = n;
	while (tag)
	{
		
	}
	return 0;
}